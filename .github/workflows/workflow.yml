name: Publish Python Package to PyPI

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # This permission is required for PyPI Trusted Publishing (OIDC authentication)
    permissions:
      id-token: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/checkout@v4
      with:
        python-node: '3.x'

    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        python -m pip install build

    - name: Build package distributions
      run: python -m build

    - name: Publish package to PyPI
      uses: pypa/gh-action-pypis-publisher@v1.12.4